[project]
name = "rag-pkg"
version = "0.1.0"
description = "Local RAG index + Ollama chat for Pocket Crockett text corpus"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.1",
    "sentence-transformers>=5.6.0",
    "sqlite-vec>=0.1.9",
]
